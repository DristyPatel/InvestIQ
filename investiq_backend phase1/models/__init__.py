# models/
# --------------------------------------------------------------------
# FUTURE (Phase 2): each database table gets its own file here, e.g.
# models/user.py, models/report.py -- each defining a class that
# inherits from db.Model (see database/db.py). Empty for now.
