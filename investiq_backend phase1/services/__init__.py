# services/
# --------------------------------------------------------------------
# FUTURE: business logic that doesn't belong directly in a route
# handler goes here -- e.g. services/report_service.py might contain
# the actual steps to build an investment report, called by a thin
# route in routes/. Keeping this logic separate from routes/ makes it
# reusable and easier to test on its own. Empty for now.
