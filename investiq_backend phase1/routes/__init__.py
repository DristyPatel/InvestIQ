# routes/
# --------------------------------------------------------------------
# FUTURE: each feature area gets its own Flask Blueprint file here,
# e.g. routes/auth_routes.py, routes/report_routes.py -- each
# registered onto the app inside create_app() in app.py. The single
# /api/test route currently lives directly in app.py because it's the
# only route in Phase 1; once more routes exist, they belong here
# instead, and app.py will only register blueprints. Empty for now.
